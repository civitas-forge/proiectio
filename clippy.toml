disallowed-methods = [
    { path = "std::fs::canonicalize", reason = "canonicalize follows symlinks and requires existence; containment is lexical — go through contained_join (docs/security.lex section 2)" },
    { path = "std::path::Path::canonicalize", reason = "canonicalize follows symlinks and requires existence; containment is lexical — go through contained_join (docs/security.lex section 2)" },
    { path = "camino::Utf8Path::canonicalize", reason = "canonicalize follows symlinks and requires existence; containment is lexical — go through contained_join (docs/security.lex section 2)" },
    { path = "camino::Utf8Path::canonicalize_utf8", reason = "canonicalize follows symlinks and requires existence; containment is lexical — go through contained_join (docs/security.lex section 2)" },
]
