{#- One line per row: the verb in its style, the path, and the note on it. -#}
{%- for row in run.rows -%}
{{ row.verb|style_as(row.style) }}{{ row.verb_pad }}  {% if row.note %}{{ row.path }}{{ row.path_pad }}  {{ row.note }}{% else %}{{ row.path }}{% endif %}
{% endfor -%}
{%- if run.summary %}{{ run.summary }}
{% endif -%}
