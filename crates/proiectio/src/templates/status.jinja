{#- One line per classified path: the state in its style, padded, then the path. -#}
{%- for row in status.rows -%}
{{ row.state|style_as(row.style) }}{{ row.state_pad }}  {{ row.path }}
{% endfor -%}
